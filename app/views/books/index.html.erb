
<div class="container">
	<div class="row">
		<!--Userプロフィール-->
		<%= render 'users/profile', user: current_user %>
		<!--book投稿フォーム-->
		<%= render 'books/form', book: @book %>
	</div>
	
	<div class="row">
		<!--books一覧 -->
		<%= render 'index', books: @books %> 
	</div>
</div>
